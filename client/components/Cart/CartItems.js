import React from 'react'


const CartItems = (cartItems) => {
    return (
        <div>
            {cartItems.length > 0} ? {cartItems.map((product, i) => {

            })}
        </div>
    )
}
